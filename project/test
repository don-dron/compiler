import sort.quick.quickSort

() int main ->
    int size = 100
    int[] a = int[size]
    int i = 0

    while i < size
        a[i] = size - i
        i++

    quickSort(a, 0, size - 1)

    return 0
