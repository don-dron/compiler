import sort.quick.quickSort

() int main ->
    int size = 100
    int[][] b = int[size][size]
    int i = 0

    while i < size
        b[i] = int[size]
        i++

    int[] a = int[size]

    i = 0

    while i < size
        a[i] = size - i
        i++


    quickSort(a, 0, size - 1)

    i = 1
    while i < size - 1
        if a[i-1] > a[i]
            return 223
        i++


    return 0
