import lang.lib

() void runnable ->
    int i = 25

    while (i > 0)
        i--
        yield()
        yield()
        yield()
        println(concat("Test", toString(i))
        yield()
        yield()
        yield()
        yield()

    return


() int main ->
    int j  = 0
    println(concat("Test", toString(2))

    while j < 300000
        Thread t = new Thread(runnable)
        j++

    return 0

